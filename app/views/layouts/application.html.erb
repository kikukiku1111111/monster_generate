<!DOCTYPE html>
<html>
  <head>
    <title>Pokemon</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
  <nav class="bg-blue">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="relative flex h-20 items-center justify-between">
      <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
        <div class="flex flex-shrink-0 items-center">
          <img class="h-16 w-auto" src="/icon.png" alt="Your Company">
        </div>
      </div>
        <div id="mypageBtnContainer" class="relative ml-3">
          <div class="relative">
            <button type="button"  class="relative flex rounded-full border border-black bg-white text-sm focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800" id="user-menu-button" aria-expanded="false" aria-haspopup="true">
              <span class="absolute -inset-1.5"></span>
              <span class="sr-only">Open user menu</span>
              <img class="h-8 w-8 rounded-full" src="/profile.png" alt="">
            </button>
          </div>
          <div id="mypageDropdown" clss="hidden absolute top-full left-0 z-999">
           <% if user_signed_in? %>
          <%= link_to("ポケモン生成","/user/pokemon") %> 
          <%= link_to("マイポケモン","/user/mypokemon") %> 
          <% end %>
          <div class="absolute right-0 z-10  w-48 origin-top-right rounded-md bg-white py-1 shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none" role="menu" aria-orientation="vertical" aria-labelledby="user-menu-button" tabindex="-1">
            
            <% if user_signed_in? %>
              <%= link_to("ログアウト", "/users/sign_out" ,class:"block px-4 py-2 text-sm text-gray-700" , data: { turbo_method: :delete }) %>
              <%= link_to("プロフィール", "/user/#{current_user.id}" ,class:"block px-4 py-2 text-sm text-gray-700") %>

            <% else %>
            <%= link_to("新規登録", "/users/sign_up" ,class:"block px-4 py-2 text-sm text-gray-700") %>
            <%= link_to("ログイン", "/users/sign_in" ,class:"block px-4 py-2 text-sm text-gray-700") %>
            <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
  </body>
      <%= yield %>
</html>
